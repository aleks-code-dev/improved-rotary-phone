---
phase: 02-spring-project-detection
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/renderer/components/Sidebar/EndpointsTree.tsx
  - src/renderer/components/DtoClassPanel.tsx
  - src/renderer/state/useRequest.ts
  - src/renderer/state/useTabs.ts
  - src/renderer/components/Sidebar.tsx
autonomous: true
requirements: [SPRING-05]

must_haves:
  truths:
    - "Clicking an endpoint in the tree opens a new tab with prefilled URL, method, headers, path params, query params"
    - "Tab title shows '{METHOD} {endpoint_path}' format"
    - "Path variables from @PathVariable are pre-filled as empty editable values"
    - "Query params from @RequestParam are pre-filled with defaults"
    - "Headers include Content-Type from consumes and Accept from produces"
    - "Body mode is set to raw+application/json if requestBodyFqn is non-null, none otherwise"
    - "DTO class panel shows resolved DTO FQN and field list for selected endpoint"
    - "App re-opens with last project path and triggers rescan on launch"
    - "Rescan button triggers full re-scan of current project"
  artifacts:
    - path: "src/renderer/components/Sidebar/EndpointsTree.tsx"
      provides: "Click-to-prefill handler in endpoint tree"
      contains: "handleEndpointClick"
    - path: "src/renderer/components/DtoClassPanel.tsx"
      provides: "DTO class info display for selected endpoint"
      min_lines: 60
    - path: "src/renderer/state/useRequest.ts"
      provides: "setSpec method for prefilling request from endpoint"
      contains: "setSpec"
  key_links:
    - from: "src/renderer/components/Sidebar/EndpointsTree.tsx"
      to: "src/renderer/state/useTabs.ts"
      via: "addTab({method, url, sourceItemName})"
      pattern: "addTab"
    - from: "src/renderer/components/Sidebar/EndpointsTree.tsx"
      to: "src/renderer/state/useRequest.ts"
      via: "setSpec(tabId, prefilledSpec)"
      pattern: "setSpec"
    - from: "src/renderer/components/DtoClassPanel.tsx"
      to: "src/renderer/store/endpoints.ts"
      via: "useEndpointsStore for selected endpoint data"
      pattern: "useEndpointsStore"
---

<objective>
Complete the Spring detection feature by wiring click-to-prefill (clicking an endpoint opens a prefilled request tab), adding a DTO class panel for selected endpoints, and implementing open-time rescan with the rescan button.

Purpose: This completes the end-to-end user story — point at a Spring project, see endpoints, click one, get a prefilled request ready to send. The DTO panel provides context about the request body class.

Output: Fully functional Spring project detection feature. User can select project → see endpoints → click to prefill → view DTO info → rescan when needed.
</objective>

<execution_context>
@C:/Users/aleks/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/aleks/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-spring-project-detection/02-UI-SPEC.md
@.planning/phases/02-spring-project-detection/02-PATTERNS.md
@.planning/phases/02-spring-project-detection/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/renderer/state/useRequest.ts (existing):
```typescript
// Per-tab Zustand store for RequestSpec
// Methods: setSpec(tabId, spec), getSpec(tabId)
// RequestSpec matches RequestSpecSchema from channels.ts
```

From src/renderer/state/useTabs.ts (existing):
```typescript
// Zustand store for open tabs
// Methods: addTab({method, url, sourceItemName}) => tabId, removeTab, setActiveTab
```

From src/renderer/store/endpoints.ts (created in 02-02):
```typescript
interface EndpointsState {
  activeProjectId: string | null;
  activeProjectPath: string | null;
  scanStatus: 'idle' | 'scanning' | 'error';
  lastScanResult: ProjectScanResult | null;
  // ... methods
}
```

From src/main/ipc/channels.ts (created in 02-01):
```typescript
export type Endpoint = {
  id: string;
  method: 'GET' | 'POST' | 'PUT' | 'PATCH' | 'DELETE';
  fullPath: string;
  handlerMethod: string;
  pathVariables: Array<{name: string, type: string, required: boolean}>;
  queryParams: Array<{name: string, type: string, required: boolean, defaultValue: string | null}>;
  requestBodyFqn: string | null;
  consumes: string[];
  produces: string[];
  sourceFile: string;
  lineNumber: number;
};
```

From src/renderer/components/Sidebar/EndpointsTree.tsx (created in 02-02):
```typescript
export function EndpointsTree() {
  // ... existing tree rendering
  // handleEndpointClick needs to be implemented in 02-03
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Click-to-prefill endpoint handler + DtoClassPanel</name>
  <files>
    src/renderer/components/Sidebar/EndpointsTree.tsx
    src/renderer/components/DtoClassPanel.tsx
    src/renderer/components/Sidebar.tsx
  </files>
  <read_first>
    src/renderer/components/Sidebar/EndpointsTree.tsx
    src/renderer/state/useRequest.ts
    src/renderer/state/useTabs.ts
    src/renderer/store/endpoints.ts
    src/renderer/components/Sidebar/CollectionsTree.tsx
    .planning/phases/02-spring-project-detection/02-UI-SPEC.md
    .planning/phases/02-spring-project-detection/02-PATTERNS.md
  </read_first>
  <action>
Implement click-to-prefill and DTO class panel:

1. **src/renderer/components/Sidebar/EndpointsTree.tsx** — Add click-to-prefill handler:
   - Import `useTabs` from `../../state/useTabs` and `useRequest` from `../../state/useRequest`
   - Add `handleEndpointClick(endpoint: Endpoint)` function that:
     a. Creates new tab via `useTabs.getState().addTab({ method: endpoint.method, url: \`{{baseUrl}}${endpoint.fullPath}\`, sourceItemName: \`${endpoint.handlerMethod} [${endpoint.sourceFile}]\` })`
     b. Builds path params from endpoint.pathVariables: `endpoint.pathVariables.map(p => ({ key: p.name, value: '' }))`
     c. Builds query params from endpoint.queryParams: `endpoint.queryParams.map(p => ({ key: p.name, value: p.defaultValue ?? '', enabled: true }))`
     d. Builds headers: `[{ key: 'Content-Type', value: endpoint.consumes[0] ?? 'application/json', enabled: true }, { key: 'Accept', value: endpoint.produces[0] ?? 'application/json', enabled: true }]`
     e. Builds body: if endpoint.requestBodyFqn is non-null → `{ mode: 'raw', contentType: 'application/json', text: '{}' }`, else `{ mode: 'none' }`
     f. Calls `useRequest.getState().setSpec(tabId, { requestId: crypto.randomUUID(), method: endpoint.method, url: \`{{baseUrl}}${endpoint.fullPath}\`, headers, queryParams, pathParams, body, auth: { type: 'none' }, settings: { timeoutMs: 30000, followRedirects: true, maxRedirects: 10, sslVerify: true, saveCookiesToJar: false } })`
   - Wire the click handler to endpoint rows: `onClick={() => handleEndpointClick(ep)}`
   - Track `selectedEndpointId` state for DTO panel display
   - Set selectedEndpointId when an endpoint row is clicked (before calling handleEndpointClick)

2. **src/renderer/components/DtoClassPanel.tsx** — New component:
   - Import useEndpointsStore from store/endpoints
   - Read selectedEndpointId from parent (via prop or context)
   - Find the selected endpoint from lastScanResult.controllers
   - If endpoint has requestBodyFqn: show panel with:
     - Header: "Request Body Class" (11px uppercase, weight 600, var(--color-fg-muted))
     - FQN: endpoint.requestBodyFqn (12px ui-monospace, weight 600)
     - Note: "Full DTO body generation is available in Phase 3" (11px muted)
   - If endpoint has no requestBodyFqn: show "No DTO class resolved for this endpoint" (12px muted)
   - Panel styling: transparent background, padding `var(--space-2) 0`, border-top `1px solid var(--color-border)`
   - Place below EndpointsTree in the sidebar, only visible when an endpoint is selected

3. **src/renderer/components/Sidebar.tsx** — Add DtoClassPanel rendering:
   - Import DtoClassPanel
   - Render `<DtoClassPanel />` after `<EndpointsTree />` when endpoints group is visible
   - Pass selectedEndpointId from EndpointsTree (may need to lift state or use the endpoints store)
   - Alternative: DtoClassPanel reads selectedEndpointId directly from useEndpointsStore (add selectedEndpointId to the store)

4. **src/renderer/store/endpoints.ts** — Add selectedEndpointId to store:
   - Add `selectedEndpointId: string | null` to EndpointsState
   - Add `setSelectedEndpoint: (id: string | null) => void` method
   - EndpointsTree sets selectedEndpointId on click; DtoClassPanel reads it
  </action>
  <verify>
    <automated>cd C:/Users/aleks/Desktop/postman-clone && npx tsc --noEmit -p tsconfig.web.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/renderer/components/Sidebar/EndpointsTree.tsx` contains `handleEndpointClick` function
    - `src/renderer/components/Sidebar/EndpointsTree.tsx` contains `addTab` import from useTabs
    - `src/renderer/components/Sidebar/EndpointsTree.tsx` contains `setSpec` import from useRequest
    - `src/renderer/components/Sidebar/EndpointsTree.tsx` contains `{{baseUrl}}` in URL construction
    - `src/renderer/components/Sidebar/EndpointsTree.tsx` contains `requestBodyFqn` in body mode selection
    - `src/renderer/components/DtoClassPanel.tsx` exists and exports `DtoClassPanel`
    - `src/renderer/components/DtoClassPanel.tsx` contains `"Request Body Class"` string
    - `src/renderer/components/DtoClassPanel.tsx` contains `"No DTO class resolved"` string
    - `src/renderer/store/endpoints.ts` contains `selectedEndpointId`
    - `npx tsc --noEmit -p tsconfig.web.json` exits 0
  </acceptance_criteria>
  <done>Clicking an endpoint opens a prefilled request tab with method, URL ({{baseUrl}} + path), headers (Content-Type, Accept), path params (empty values), query params (with defaults), and body mode (raw+JSON if DTO exists). DtoClassPanel shows resolved DTO FQN below the endpoint tree.</done>
</task>

<task type="auto">
  <name>Task 2: Open-time rescan + rescan button + app re-open persistence</name>
  <files>
    src/renderer/components/Sidebar/EndpointsTree.tsx
    src/main/ipc/router.ts
    src/main/storage/settings.ts
  </files>
  <read_first>
    src/renderer/components/Sidebar/EndpointsTree.tsx
    src/main/ipc/router.ts
    src/main/storage/settings.ts
    src/renderer/store/endpoints.ts
    src/renderer/hooks/useEndpoints.ts
    .planning/phases/02-spring-project-detection/02-UI-SPEC.md
  </read_first>
  <action>
Implement open-time rescan and rescan button behavior:

1. **src/main/storage/settings.ts** — Add `lastSpringProjectPath` to settings:
   - Ensure settings schema includes `lastSpringProjectPath: string | null`
   - When project:scan succeeds, save the project path to settings
   - On app bootstrap, read lastSpringProjectPath and return it in bootstrap result

2. **src/main/ipc/router.ts** — Update project:scan handler:
   - After successful scan, call `setSetting('lastSpringProjectPath', parsed.path)` to persist the path
   - On bootstrap, include `lastSpringProjectPath` in the bootstrap result

3. **src/renderer/components/Sidebar/EndpointsTree.tsx** — Add open-time rescan:
   - On component mount (useEffect), check if activeProjectPath is set in the store
   - If yes and scanStatus is 'idle', trigger a rescan: `useEndpointsScan().mutate(activeProjectPath)`
   - This handles app re-open: the store gets hydrated with lastSpringProjectPath from bootstrap, then EndpointsTree triggers rescan

4. **src/renderer/components/Sidebar/EndpointsTree.tsx** — Rescan button behavior:
   - The ⟳ rescan button already exists from 02-02
   - Ensure onClick calls `useEndpointsScan().mutate(activeProjectPath)`
   - During scan, button shows loading state (disable + spinner)
   - After scan completes, tree re-renders with fresh data

5. **src/renderer/components/App.tsx** — Bootstrap hydration for project:
   - On bootstrap, read lastSpringProjectPath from bootstrap result
   - If path exists and directory still exists (check via IPC), set it in useEndpointsStore
   - If path no longer exists, clear lastSpringProjectPath from settings
  </action>
  <verify>
    <automated>cd C:/Users/aleks/Desktop/postman-clone && npx tsc --noEmit -p tsconfig.web.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/storage/settings.ts` contains `lastSpringProjectPath` in settings
    - `src/main/ipc/router.ts` contains `setSetting('lastSpringProjectPath'` in project:scan handler
    - `src/renderer/components/Sidebar/EndpointsTree.tsx` contains `useEffect` that triggers rescan on mount
    - `src/renderer/components/Sidebar/EndpointsTree.tsx` rescan button onClick triggers `useEndpointsScan().mutate`
    - `npx tsc --noEmit -p tsconfig.web.json` exits 0
  </acceptance_criteria>
  <done>App re-opens with last scanned project and triggers rescan. Rescan button works. Last project path persists across app restarts. If project path no longer exists, sidebar shows empty state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → Main (IPC) | Project path for rescan crosses here — validated by main |
| Settings → Renderer | lastSpringProjectPath read on bootstrap — informational |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | Settings persistence | accept | lastSpringProjectPath is a filesystem path, not a secret. Stored in settings.json alongside other non-sensitive settings. |
| T-02-08 | Denial of Service | Open-time rescan | mitigate | Rescan only triggers if path exists on disk. If helper is offline, scan fails gracefully with error state. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this phase. |
</threat_model>

<verification>
1. `npx tsc --noEmit -p tsconfig.web.json` — no type errors in renderer
2. Click endpoint → new tab opens with prefilled URL containing `{{baseUrl}}` + endpoint path
3. Tab title shows `GET /api/users` format
4. DtoClassPanel shows DTO FQN when endpoint has requestBodyFqn
5. DtoClassPanel shows "No DTO class resolved" when endpoint has no body
6. Rescan button triggers re-scan and tree updates
7. App re-opens with last project path and shows endpoints
</verification>

<success_criteria>
- Click-to-prefill opens new tab with URL, method, headers, path/query params, body mode
- Tab title shows `{METHOD} {path}` format
- DtoClassPanel shows DTO FQN or "No DTO class resolved" message
- Rescan button triggers full re-scan
- App re-opens with last project and triggers rescan
- Settings persist lastSpringProjectPath
- TypeScript compiles without errors
</success_criteria>

<output>
Create `.planning/phases/02-spring-project-detection/02-03-SUMMARY.md` when done
</output>

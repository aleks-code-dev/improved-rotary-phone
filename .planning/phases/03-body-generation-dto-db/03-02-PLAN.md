---
phase: 03-body-generation-dto-db
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - helper/build.gradle.kts
  - helper/src/main/java/com/postmanclone/helper/db/DbConnectionManager.java
  - helper/src/main/java/com/postmanclone/helper/db/ConnectionPoolFactory.java
  - helper/src/main/java/com/postmanclone/helper/db/TableEnumerator.java
  - helper/src/main/java/com/postmanclone/helper/db/type/TypeNormalizer.java
  - helper/src/main/java/com/postmanclone/helper/db/type/PostgresTypeNormalizer.java
  - helper/src/main/java/com/postmanclone/helper/db/type/MySqlTypeNormalizer.java
  - helper/src/main/java/com/postmanclone/helper/db/type/OracleTypeNormalizer.java
  - helper/src/main/java/com/postmanclone/helper/db/type/H2TypeNormalizer.java
  - helper/src/main/java/com/postmanclone/helper/HelperJsonRpcServer.java
  - src/main/ipc/safeStorage.ts
  - src/main/storage/db-connections.ts
  - src/shared/schemas/db-connection.ts
  - src/main/ipc/channels.ts
  - src/main/ipc/router.ts
  - src/preload/index.ts
  - src/renderer/components/Database/DbConnectionForm.tsx
autonomous: true
requirements:
  - DB-01
  - DB-02
  - DB-07

must_haves:
  truths:
    - "User can create a DB connection with JDBC URL, username, password, and test it"
    - "DB password is encrypted via Electron safeStorage before writing to disk"
    - "Plaintext password is never written to disk, never logged, never sent off-device"
    - "User can list tables and their columns for the connected database"
    - "Connection survives a helper restart (pool reconnects)"
  artifacts:
    - path: "src/main/ipc/safeStorage.ts"
      provides: "Wraps Electron safeStorage async API"
      exports: ["isAvailable()", "encryptCredential()", "decryptCredential()"]
    - path: "src/main/storage/db-connections.ts"
      provides: "CRUD for db-connections/*.json with encrypted passwords"
      exports: ["listConnections()", "readConnection()", "createConnection()", "deleteConnection()"]
    - path: "helper/.../db/DbConnectionManager.java"
      provides: "HikariCP pool lifecycle — connect, disconnect, test, listTables"
      exports: ["connect()", "disconnect()", "testConnection()", "listTables()"]
    - path: "src/renderer/components/Database/DbConnectionForm.tsx"
      provides: "JDBC URL auto-parse + connection form + test button"
  key_links:
    - from: "src/renderer/components/Database/DbConnectionForm.tsx"
      to: "src/main/ipc/router.ts"
      via: "window.api.db.connections.create → ipcRenderer.invoke('db:connections:create')"
      pattern: "db:connections:create"
    - from: "src/main/ipc/router.ts"
      to: "src/main/storage/db-connections.ts"
      via: "dbConnectionsService.createConnection()"
      pattern: "createConnection"
    - from: "src/main/ipc/router.ts"
      to: "helper/.../HelperJsonRpcServer.java"
      via: "supervisor.getClient().request('db:connect', {connId, url, user, password})"
      pattern: "db:connect"
    - from: "src/main/ipc/safeStorage.ts"
      to: "Electron safeStorage"
      via: "safeStorage.encryptStringAsync(plainText)"
      pattern: "encryptStringAsync"
---

<objective>
DB connection management (DB-01, DB-02, DB-07)

Purpose: User can create database connections (PostgreSQL, MySQL, Oracle, H2) with credentials encrypted via Electron safeStorage. The helper manages HikariCP connection pools (size 2 for desktop) and exposes table/column enumeration via JSON-RPC.

Output: safeStorage wrapper, db-connections storage layer, IPC channels for DB CRUD + connect + listTables, DbConnectionForm UI component, JVM helper DbConnectionManager + TypeNormalizer strategy.
</objective>

<execution_context>
@C:/Users/aleks/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/aleks/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-body-generation-dto-db/03-CONTEXT.md
@.planning/phases/03-body-generation-dto-db/03-RESEARCH.md
@.planning/phases/03-body-generation-dto-db/03-PATTERNS.md
@.planning/phases/03-body-generation-dto-db/03-UI-SPEC.md
@.planning/phases/03-body-generation-dto-db/03-01-SUMMARY.md

<interfaces>
<!-- From 03-01 (created by prior plan): -->

From src/main/jvm/supervisor.ts:
```typescript
export class Supervisor extends EventEmitter {
  getClient(): JsonRpcClientImpl | null; // added in 03-01
  // ...
}
```

From src/main/ipc/channels.ts:
```typescript
// Phase 3 DTO schemas (added in 03-01):
export const DtoGenerateArgsSchema = z.object({ requestId: z.string().uuid(), dtoFqn: z.string().min(1), subtypeName: z.string().optional() });
export const DtoGenerateResultSchema = z.object({ ok: z.boolean(), bodyJson: z.string(), warnings: ..., cycleRefs: ... });
```

From src/main/storage/collections.ts (analog for db-connections pattern):
```typescript
export async function listCollections(): Promise<CollectionMeta[]>;
export async function readCollection(id: string): Promise<Collection>;
export async function createCollection(name: string): Promise<{ id: string }>;
export async function deleteCollection(id: string): Promise<void>;
```

From src/main/storage/atomicWrite.ts:
```typescript
export async function writeFileAtomic(filePath: string, data: string): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: safeStorage wrapper + DB connections storage + IPC schemas</name>
  <files>
    src/main/ipc/safeStorage.ts,
    src/shared/schemas/db-connection.ts,
    src/main/storage/db-connections.ts,
    src/main/ipc/channels.ts,
    src/main/ipc/router.ts,
    src/preload/index.ts
  </files>
  <action>
1. Create `src/main/ipc/safeStorage.ts` (per PATTERNS.md §4):
   - `import { safeStorage } from 'electron'`
   - `export function isAvailable(): boolean` — returns `safeStorage.isEncryptionAvailable()`
   - `export async function encryptCredential(plainText: string): Promise<Buffer>` — checks isAvailable(), calls `safeStorage.encryptStringAsync(plainText)`, throws if unavailable
   - `export async function decryptCredential(encrypted: Buffer): Promise<string>` — calls `safeStorage.decryptStringAsync(encrypted)`, returns `result`. If `shouldReEncrypt` is true, re-encrypt and return (caller stores re-encrypted value)
   - Follow the `jdkDetect.ts` pattern: standalone utility with exported functions, no class

2. Create `src/shared/schemas/db-connection.ts`:
   - Zod schema: `DbConnectionSchema = z.object({ id: z.string().uuid(), name: z.string(), url: z.string(), user: z.string(), passwordEncrypted: z.string(), // base64-encoded Buffer dbType: z.enum(['postgresql', 'mysql', 'oracle', 'h2']), createdAt: z.number() })`
   - Export type: `DbConnection = z.infer<typeof DbConnectionSchema>`
   - Meta schema (no password): `DbConnectionMetaSchema = z.object({ id: z.string().uuid(), name: z.string(), dbType: z.string(), connected: z.boolean().default(false) })`

3. Create `src/main/storage/db-connections.ts` (per PATTERNS.md §5):
   - `import { writeFileAtomic } from './atomicWrite.js'`
   - `import { encryptCredential, decryptCredential } from '../ipc/safeStorage.js'`
   - `import { DbConnectionSchema, type DbConnection } from '../../shared/schemas/db-connection.js'`
   - Storage path: `path.join(getDataDir(), 'db-connections', id, 'connection.json')`
   - `listConnections()`: reads all dirs in `db-connections/`, returns metadata (name, dbType, id) — no passwords
   - `readConnection(id)`: reads JSON, validates with Zod, decrypts password via `decryptCredential(Buffer.from(passwordEncrypted, 'base64'))`, returns full connection with plaintext password (for relay to helper)
   - `createConnection(name, url, user, password, dbType)`: generates UUID, encrypts password via `encryptCredential(password)`, stores base64-encoded encrypted buffer, writes atomic JSON
   - `deleteConnection(id)`: `fs.rm(dir, { recursive: true, force: true })`
   - `updateConnection(id, partial)`: read, merge, re-encrypt if password changed, write atomic

4. Add DB IPC schemas to `channels.ts` (per PATTERNS.md §6):
   - `DbConnectionCreateArgsSchema`: `{ name: z.string().min(1).max(200), url: z.string().min(1), user: z.string(), password: z.string(), dbType: z.enum(['postgresql', 'mysql', 'oracle', 'h2']) }`
   - `DbConnectionDeleteArgsSchema`: `{ id: z.string().uuid() }`
   - `DbConnectionListResultSchema`: `z.array(DbConnectionMetaSchema)`
   - `DbConnectArgsSchema`: `{ connectionId: z.string().uuid() }`
   - `DbConnectResultSchema`: `{ ok: z.boolean(), status: z.enum(['connected', 'error']), error: z.string().optional(), tables: z.number().optional() }`
   - `DbTestConnectionArgsSchema`: `{ url: z.string().min(1), user: z.string(), password: z.string(), dbType: z.enum(['postgresql', 'mysql', 'oracle', 'h2']) }`
   - `DbListTablesArgsSchema`: `{ connectionId: z.string().uuid() }`
   - `DbTableInfoSchema`: `{ name: z.string(), schema: z.string().nullable(), columnCount: z.number(), rowCountEstimate: z.number() }`
   - `DbListTablesResultSchema`: `z.array(DbTableInfoSchema)`
   - Export all inferred types

5. Register IPC handlers in `router.ts` (per PATTERNS.md §7):
   - `db:connections:list` → calls `dbConnectionsService.listConnections()`
   - `db:connections:create` → parses args, calls `dbConnectionsService.createConnection()`
   - `db:connections:delete` → parses args, calls `dbConnectionsService.deleteConnection()`
   - `db:connect` → reads connection (decrypts password), relays to helper via `supervisor.getClient().request('db:connect', { connId, url, user, password })`
   - `db:disconnect` → relays to helper
   - `db:testConnection` → relays to helper with plaintext password (for test only, never persisted)
   - `db:listTables` → relays to helper
   - All handlers: try/catch with `log.error()`, return `{ ok: false, error: '...' }` — NEVER include passwords in error messages

6. Add to preload `index.ts`:
   - Add `db` section to `WindowApi` interface:
     ```
     db: {
       connections: { list: () => Promise<any>; create: (args) => Promise<any>; delete: (args) => Promise<any> };
       connect: (args) => Promise<any>;
       disconnect: (args) => Promise<any>;
       testConnection: (args) => Promise<any>;
       listTables: (args) => Promise<any>;
     }
     ```
   - Add implementation mapping each to `ipcRenderer.invoke('db:...')`
  </action>
  <verify>
    <automated>cd src && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - safeStorage encrypts/decrypts credentials using Electron's async API
    - db-connections storage CRUD works: create stores encrypted password, read decrypts, delete removes
    - IPC channels registered: db:connections:list/create/delete, db:connect/disconnect, db:testConnection, db:listTables
    - Preload exposes window.api.db to renderer
    - Passwords never appear in logs or error messages
    - TypeScript compiles
  </done>
</task>

<task type="auto">
  <name>Task 2: JVM helper DB connection manager + table enumerator + type normalizers</name>
  <files>
    helper/build.gradle.kts,
    helper/src/main/java/com/postmanclone/helper/db/type/TypeNormalizer.java,
    helper/src/main/java/com/postmanclone/helper/db/type/PostgresTypeNormalizer.java,
    helper/src/main/java/com/postmanclone/helper/db/type/MySqlTypeNormalizer.java,
    helper/src/main/java/com/postmanclone/helper/db/type/OracleTypeNormalizer.java,
    helper/src/main/java/com/postmanclone/helper/db/type/H2TypeNormalizer.java,
    helper/src/main/java/com/postmanclone/helper/db/ConnectionPoolFactory.java,
    helper/src/main/java/com/postmanclone/helper/db/DbConnectionManager.java,
    helper/src/main/java/com/postmanclone/helper/db/TableEnumerator.java,
    helper/src/main/java/com/postmanclone/helper/HelperJsonRpcServer.java,
    src/renderer/components/Database/DbConnectionForm.tsx
  </files>
  <action>
1. Add JDBC driver + HikariCP dependencies to `helper/build.gradle.kts` (per RESEARCH.md §Standard Stack):
   - `implementation("com.zaxxer:HikariCP:6.2.1")`
   - `implementation("org.postgresql:postgresql:42.7.11")`
   - `implementation("com.mysql:mysql-connector-j:9.1.0")`
   - `implementation("com.oracle.database.jdbc:ojdbc11:23.7.0.25.01")`
   - `implementation("com.h2database:h2:2.3.232")`

2. Create `TypeNormalizer.java` interface (per RESEARCH.md §Pattern 5):
   - `public interface TypeNormalizer { Object normalize(ResultSet rs, int columnIndex, String columnTypeName) throws SQLException; }`

3. Create four implementations (per RESEARCH.md §Pitfall 4 + §Pitfall 6):
   - `PostgresTypeNormalizer`: checks `instanceof PGobject`, if type is "json"/"jsonb" re-parses to Jackson JsonNode via `new ObjectMapper().readTree(pg.getValue())`
   - `MySqlTypeNormalizer`: MySQL JSON returns as String, parse to JsonNode
   - `OracleTypeNormalizer`: Oracle JSON may return as CLOB or String, read and parse to JsonNode
   - `H2TypeNormalizer`: H2 returns JSON as String, parse to JsonNode
   - For unknown types in all normalizers: return raw value as-is

4. Create `ConnectionPoolFactory.java` (per RESEARCH.md §HikariCP Desktop Configuration):
   - `public static HikariDataSource create(String url, String user, String password)` method
   - Desktop overrides per RESEARCH.md:
     - `maximumPoolSize=2`
     - `minimumIdle=1`
     - `connectionTimeout=10_000` (10s)
     - `idleTimeout=300_000` (5min)
     - `maxLifetime=900_000` (15min)
     - `keepaliveTime=120_000` (2min)
   - Returns `HikariDataSource`

5. Create `DbConnectionManager.java` (per PATTERNS.md §2 analog — resource lifecycle):
   - `private final Map<String, HikariDataSource> pools = new ConcurrentHashMap<>()`
   - `public void connect(String connId, String url, String user, String password)` — creates pool via ConnectionPoolFactory, stores in map
   - `public void disconnect(String connId)` — calls `pool.close()`, removes from map
   - `public boolean testConnection(String url, String user, String password)` — creates temporary pool, tests `connection.isValid(5)`, closes, returns true/false
   - `public HikariDataSource getPool(String connId)` — returns pool or null
   - Password handling: clear char array after DataSource creation (per RESEARCH.md §Security — memory dump mitigation)

6. Create `TableEnumerator.java`:
   - `public List<Map<String, Object>> listTables(HikariDataSource pool)` — uses `DatabaseMetaData.getTables(null, null, "%", new String[]{"TABLE"})` to get table names, schemas, types
   - For each table: query `SELECT COUNT(*) FROM tableName` for row count estimate (cap at 10000 for performance)
   - Returns list of `{ name, schema, columnCount, rowCountEstimate }`
   - `public List<Map<String, Object>> listColumns(HikariDataSource pool, String tableName)` — uses `DatabaseMetaData.getColumns(null, null, tableName, "%")` for column name, type, nullable

7. Extend `HelperJsonRpcServer.java` dispatch loop with DB RPC methods:
   - `"db:connect"` → extracts connId, url, user, password; calls `dbConnectionManager.connect(connId, url, user, password)`; returns `{ status: "connected" }`
   - `"db:disconnect"` → extracts connId; calls `dbConnectionManager.disconnect(connId)`; returns `{ status: "disconnected" }`
   - `"db:testConnection"` → extracts url, user, password; calls `dbConnectionManager.testConnection()`; returns `{ connected: true/false, latencyMs: N }`
   - `"db:listTables"` → extracts connId; gets pool, calls `tableEnumerator.listTables(pool)`; returns array of table info
   - `"db:parseJdbcUrl"` → extracts url; parses JDBC URL into components (driver, host, port, database); returns parsed object. Known patterns: `jdbc:postgresql:`, `jdbc:mysql:`, `jdbc:oracle:`, `jdbc:h2:`
   - All DB methods: try/catch, NEVER log passwords, return JSON-RPC error on failure

8. Create `DbConnectionForm.tsx` (per UI-SPEC §3):
   - Component: `DbConnectionForm` with props `{ onConnectionCreated?: () => void }`
   - Form fields: Connection Name (text input), JDBC URL (monospace textarea), Username (text), Password (password with reveal toggle 👁)
   - JDBC URL auto-parse: on URL change (debounced 300ms), call `window.api.db.parseJdbcUrl({url})` and show parsed grid (driver/host/port/database) in 2-column CSS grid (per UI-SPEC §3 layout)
   - Parsed grid labels: uppercase 10px/600 `--color-fg-muted`, values: 12px/400 `--color-fg` in `--font-mono`
   - "Test Connection" button: calls `window.api.db.testConnection(...)`, shows spinner "Testing...", result: green checkmark "Connected in Nms" or red "Connection failed: {reason}"
   - "Save Connection" button: calls `window.api.db.connections.create(...)`, on success calls `onConnectionCreated`
   - All styling uses existing CSS tokens from UI-SPEC
   - Unparseable URL: parsed grid shows "Unknown driver" in muted text, Test button remains enabled
  </action>
  <verify>
    <automated>cd helper && ./gradlew shadowJar 2>&1 | tail -5</automated>
  </verify>
  <done>
    - HikariCP pool created with desktop overrides (maxSize=2, timeout=10s)
    - 4 JDBC drivers bundled in helper fat-jar
    - TypeNormalizer interface with Postgres/MySQL/Oracle/H2 implementations
    - DbConnectionManager manages pool lifecycle (connect/disconnect/test)
    - TableEnumerator returns table list with column count and row estimate
    - Helper dispatches db:connect, db:disconnect, db:testConnection, db:listTables, db:parseJdbcUrl
    - DbConnectionForm renders JDBC URL auto-parse grid, Test Connection, Save Connection
    - Java compiles via gradlew shadowJar
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→main | IPC payloads cross process boundary; Zod validates at handler entry |
| main→helper | DB password decrypted in main, relayed to helper over stdio JSON-RPC |
| helper→database | JDBC connection to user's DB; read-only queries preferred |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | DB password in JSON file | mitigate | safeStorage.encryptStringAsync() before write; base64-encoded encrypted buffer in JSON |
| T-03-05 | Information Disclosure | DB password in logs | mitigate | Never log password; sanitize all DB error messages before logging |
| T-03-06 | Information Disclosure | DB password in helper memory | mitigate | Clear password char array after HikariDataSource creation |
| T-03-07 | Tampering | SQL injection via user WHERE clause | mitigate | PreparedStatement for all user-provided WHERE; never concatenate strings (addressed in 03-03) |
| T-03-08 | Elevation of Privilege | JDBC driver classpath injection | mitigate | Drivers bundled in fat-jar; no dynamic classpath loading |
| T-03-SC | Tampering | Maven dependency install | mitigate | All deps verified in STACK.md via Context7; no new npm packages |
</threat_model>

<verification>
1. safeStorage encrypts a string, decrypts it back to original
2. DB connection CRUD: create stores encrypted password, read decrypts correctly
3. HikariCP connects to a test H2 database and lists tables
4. Test Connection button shows latency on success, error on failure
5. DbConnectionForm parses JDBC URL into components
6. Password never appears in log files or error messages
7. Java compiles via gradlew shadowJar
8. TypeScript compiles
</verification>

<success_criteria>
- DB-01: User can connect to PostgreSQL/MySQL/Oracle/H2 via JDBC
- DB-02: App lists available tables and their columns
- DB-07: DB credentials stored locally via safeStorage, never sent off-device or logged
</success_criteria>

<output>
Create `.planning/phases/03-body-generation-dto-db/03-02-SUMMARY.md` when done
</output>

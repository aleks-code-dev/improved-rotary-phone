---
phase: 04-workflow-chains-response-mapping
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/renderer/components/Chain/ChainDataPanel.tsx
  - src/renderer/components/Chain/ChainStepColumn.tsx
  - src/renderer/components/Chain/PreviewResolvedModal.tsx
  - src/renderer/components/Chain/ChainValidationBanner.tsx
  - src/renderer/components/Chain/UnresolvedRefWarning.tsx
  - src/renderer/components/Chain/ChainEditor.tsx
autonomous: true
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-04

must_haves:
  truths:
    - "User can browse prior step responses as expandable JSON trees in the bottom data panel"
    - "Each field in the data panel has a 'Copy path' button that copies {{stepN.response.body.path}} to clipboard"
    - "User can click 'Preview Resolved' to see the body with all references replaced by resolved values"
    - "Preview shows resolved URL, headers, and body in a read-only Monaco editor"
    - "Unresolved references in preview are highlighted yellow with strikethrough"
    - "Validation banner shows when chain has issues (empty URL, invalid ref, circular ref)"
    - "Step result area shows unresolved reference warnings inline"
    - "Data panel is collapsible with chevron toggle"
    - "Data panel shows 'No previous step data' when selected step is step 1 or prior steps haven't run"
  artifacts:
    - path: "src/renderer/components/Chain/ChainDataPanel.tsx"
      provides: "Collapsible bottom panel with prior step response columns"
      exports: ["ChainDataPanel"]
    - path: "src/renderer/components/Chain/ChainStepColumn.tsx"
      provides: "Single step response data as expandable JSON tree with copy-path buttons"
      exports: ["ChainStepColumn"]
    - path: "src/renderer/components/Chain/PreviewResolvedModal.tsx"
      provides: "Modal showing resolved URL, headers, body with all references substituted"
      exports: ["PreviewResolvedModal"]
    - path: "src/renderer/components/Chain/ChainValidationBanner.tsx"
      provides: "Yellow banner listing validation issues before chain run"
      exports: ["ChainValidationBanner"]
    - path: "src/renderer/components/Chain/UnresolvedRefWarning.tsx"
      provides: "Inline warning for unresolved references in step results"
      exports: ["UnresolvedRefWarning"]
  key_links:
    - from: "src/renderer/components/Chain/ChainDataPanel.tsx"
      to: "src/renderer/components/Chain/ChainStepColumn.tsx"
      via: "renders ChainStepColumn for each prior step"
      pattern: "ChainStepColumn"
    - from: "src/renderer/components/Chain/ChainStepColumn.tsx"
      to: "navigator.clipboard.writeText"
      via: "copy-path button copies {{stepN.response.body.path}}"
      pattern: "clipboard"
    - from: "src/renderer/components/Chain/PreviewResolvedModal.tsx"
      to: "src/preload/index.ts"
      via: "window.api.chains.previewResolved({ collectionId, chainId, stepIndex })"
      pattern: "chains:previewResolved"
    - from: "src/renderer/components/Chain/ChainEditor.tsx"
      to: "src/renderer/components/Chain/ChainDataPanel.tsx"
      via: "renders ChainDataPanel below ChainRequestBuilder"
      pattern: "ChainDataPanel"
---

<objective>
Response→body mapping editor + preview resolved body (MAP-01, MAP-02, MAP-03, MAP-04)

Purpose: Build the bottom data panel showing prior step responses as expandable JSON trees with click-to-copy reference paths, the preview resolved modal showing body with all {{stepN...}} references substituted, validation banner for pre-run issues, and unresolved reference warnings inline in step results.

Output: ChainDataPanel, ChainStepColumn, PreviewResolvedModal, ChainValidationBanner, UnresolvedRefWarning components. Integration into ChainEditor from 04-02.
</objective>

<execution_context>
@C:/Users/aleks/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/aleks/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-workflow-chains-response-mapping/04-CONTEXT.md
@.planning/phases/04-workflow-chains-response-mapping/04-RESEARCH.md
@.planning/phases/04-workflow-chains-response-mapping/04-UI-SPEC.md
@.planning/phases/04-workflow-chains-response-mapping/04-01-SUMMARY.md
@.planning/phases/04-workflow-chains-response-mapping/04-02-SUMMARY.md

<interfaces>
<!-- From 04-01 (chain schemas + IPC): -->

From src/shared/schemas/collection.ts:
```typescript
export type StepResult = z.infer<typeof StepResultSchema>;
// StepResult has: status, statusText, headers, bodyBase64, bodyTruncated, bodySizeBytes, timing, completedAt, unresolvedRefs, retryAttempts
```

From src/preload/index.ts:
```typescript
chains: {
  previewResolved: (args: { collectionId: string; chainId: string; stepIndex: number }) => Promise<{
    resolvedUrl: string;
    resolvedHeaders: Array<{ key: string; value: string }>;
    resolvedBody: string;
    warnings: Array<{ reference: string; reason: string }>;
  }>;
  validate: (args: { collectionId: string; chainId: string }) => Promise<{
    valid: boolean;
    issues: Array<{ type: string; message: string; stepIndex?: number }>;
  }>;
}
```

<!-- From 04-02 (chain editor components): -->

From src/renderer/components/Chain/ChainEditor.tsx:
```typescript
// ChainEditor renders: ChainHeader + StepSequence + ChainRequestBuilder
// ChainEditor has: chain (full Chain object), selectedStepIndex, stepResults (Map), isRunning
```

From src/renderer/state/useChain.ts:
```typescript
export const useChain = create<ChainState>((set, get) => ({
  activeChainId: string | null;
  selectedStepIndex: number;
  isRunning: boolean;
  stepResults: Map<number, any>;
  validationIssues: Array<...> | null;
  // ... actions
}));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChainStepColumn + ChainDataPanel + UnresolvedRefWarning</name>
  <read_first>
    src/renderer/components/ResponseViewer/BodyTab.tsx,
    src/renderer/components/Chain/ChainEditor.tsx,
    src/renderer/state/useChain.ts,
    src/shared/schemas/collection.ts,
    .planning/phases/04-workflow-chains-response-mapping/04-UI-SPEC.md
  </read_first>
  <files>
    src/renderer/components/Chain/ChainStepColumn.tsx,
    src/renderer/components/Chain/ChainDataPanel.tsx,
    src/renderer/components/Chain/UnresolvedRefWarning.tsx
  </files>
  <action>
1. Create `src/renderer/components/Chain/ChainStepColumn.tsx` (per UI-SPEC §5 Bottom Data Panel, RESEARCH.md §Pattern 4):
   - Props: `{ stepIndex: number; method: string; url: string; result: StepResult; onCopyPath: (pathExpression: string) => void }`
   - Layout: single column showing one prior step's response data
     - Column: `flex: 1; min-width: 200px; background: var(--color-bg-elevated); border: 1px solid var(--color-border); border-radius: var(--radius-1); overflow: auto`
     - Column title: "Step {N} · {METHOD} {url} · {status}" — 10px/600 uppercase
       - Status color: `var(--color-step-success)` for 2xx, `var(--color-step-failed)` for non-2xx
     - Data rows: key-value pairs from the response body
   - Parse response body: `JSON.parse(atob(result.bodyBase64))` — if fails, show raw string
   - Render JSON tree recursively:
     - For each key-value pair: render row with key (10px `--font-mono`, `--color-fg-muted`), value (10px `--font-mono`, `--color-fg`)
     - Objects/arrays: collapsible with ▶/▼ chevron, indent children with `padding-left: var(--space-3)`
     - Leaf values: show actual value (string in quotes, number, boolean, null)
     - Each leaf row gets a "Copy path" button on hover:
       - 9px, `--color-fg-muted` border, `padding: 1px 4px`, `border-radius: var(--radius-1)`
       - On click: copy `{{step{stepIndex}.response.body.{path}}}` to clipboard via `navigator.clipboard.writeText`
       - Path construction: join keys with dots, arrays with `[index]` — e.g., `items[0].id`
       - Show brief "Copied" tooltip for 1.5s after copy
   - Also show headers as top-level expandable node:
     - Key: header name, value: header value
     - "Copy path" copies `{{step{stepIndex}.response.headers.{Header-Name}}}`
   - Show status as top-level node:
     - "Copy path" copies `{{step{stepIndex}.response.status}}`
   - Reuse JSON parsing logic from `ResponseBodyTab` but render as tree (not Monaco)

2. Create `src/renderer/components/Chain/ChainDataPanel.tsx` (per UI-SPEC §5):
   - Props: `{ steps: ChainStep[]; selectedStepIndex: number; stepResults: Map<number, any> }`
   - Collapsible bottom panel below the request builder
   - Panel header: chevron `▶` (rotates 90° when open) + "Previous Step Responses" label (12px/600 Inter) + "{N} steps available" count (10px `--color-fg-muted`)
   - Click header to toggle open/closed
   - Panel: `border: 1px solid var(--color-border); border-radius: var(--radius-2); background: var(--color-bg-elevated); margin-top: var(--space-3)`
   - Columns container: `display: flex; gap: var(--space-3); overflow-x: auto; padding: var(--space-3)`
   - Render `ChainStepColumn` for each step before the selected step that has a result
   - Default state: open when selected step has prior steps with data; closed when no prior data
   - Empty state when no prior data: "No previous step data — Run earlier steps to populate data for reference expressions." in `--color-fg-muted`, 12px, centered
   - Collapsible animation: `max-height` transition (200ms ease)

3. Create `src/renderer/components/Chain/UnresolvedRefWarning.tsx` (per UI-SPEC §8):
   - Props: `{ unresolvedRefs: string[] }`
   - Only render when unresolvedRefs.length > 0
   - Layout: yellow warning box
     - `background: rgba(251,191,36,0.1); border-left: 3px solid var(--color-warning); padding: var(--space-2) var(--space-3); font-size: 12px; margin-top: var(--space-2)`
   - Content:
     - "⚠ {N} unresolved reference(s):" in 12px/600
     - List each ref: "• {reference} — step has not run or path does not exist" in 12px
     - "Empty strings were substituted. The chain continued." in 10px `--color-fg-muted`

4. Update `src/renderer/components/Chain/ChainEditor.tsx` (from 04-02):
   - Import `ChainDataPanel` and `UnresolvedRefWarning`
   - Add `ChainDataPanel` below `ChainRequestBuilder` in the layout
   - Pass steps, selectedStepIndex, stepResults to ChainDataPanel
   - Add `UnresolvedRefWarning` in the step result area (below the request builder, above the data panel)
   - Pass selected step's lastResult?.unresolvedRefs to UnresolvedRefWarning
  </action>
  <verify>
    <automated>cd C:\Users\aleks\Desktop\postman-clone && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/renderer/components/Chain/ChainStepColumn.tsx` renders expandable JSON tree with copy-path buttons
    - Copy-path button copies `{{stepN.response.body.path}}` to clipboard
    - `src/renderer/components/Chain/ChainDataPanel.tsx` renders collapsible panel with step columns
    - Data panel shows "No previous step data" empty state when no prior results
    - `src/renderer/components/Chain/UnresolvedRefWarning.tsx` shows yellow warning with ref list
    - ChainEditor integrates ChainDataPanel and UnresolvedRefWarning
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>
    - ChainStepColumn renders expandable JSON tree with per-field copy-path buttons
    - Copy-path constructs correct {{stepN.response.body.path}} expression
    - ChainDataPanel is collapsible with chevron toggle, shows prior step columns
    - Data panel shows empty state when no prior data
    - UnresolvedRefWarning shows yellow box with unresolved ref list
    - ChainEditor integrates data panel and warnings
    - TypeScript compiles
  </done>
</task>

<task type="auto">
  <name>Task 2: PreviewResolvedModal + ChainValidationBanner + final integration</name>
  <read_first>
    src/renderer/components/Chain/ChainEditor.tsx,
    src/renderer/components/Chain/ChainHeader.tsx,
    src/renderer/components/Chain/ChainRequestBuilder.tsx,
    src/renderer/state/useChain.ts,
    src/preload/index.ts,
    .planning/phases/04-workflow-chains-response-mapping/04-UI-SPEC.md
  </read_first>
  <files>
    src/renderer/components/Chain/PreviewResolvedModal.tsx,
    src/renderer/components/Chain/ChainValidationBanner.tsx,
    src/renderer/components/Chain/ChainEditor.tsx,
    src/renderer/components/Chain/ChainHeader.tsx
  </files>
  <action>
1. Create `src/renderer/components/Chain/PreviewResolvedModal.tsx` (per UI-SPEC §6, D-17, MAP-04):
   - Props: `{ isOpen: boolean; onClose: () => void; collectionId: string; chainId: string; stepIndex: number; stepName: string; stepMethod: string; stepUrl: string }`
   - Modal overlay: `position: fixed; inset: 0; background: rgba(0,0,0,0.5); display: flex; align-items: center; justify-content: center; z-index: 1000`
   - Modal content: `background: var(--color-bg-elevated); border: 1px solid var(--color-border); border-radius: var(--radius-2); width: 600px; max-height: 80vh; overflow: auto; padding: var(--space-5)`
   - Header: "Preview Resolved Body" (16px/600) + close button `✕`
   - Step info: "Step {N} · {METHOD} {url}" in 12px `--color-fg-muted`
   - Resolved URL section:
     - Label: "Resolved URL:" in 12px/600
     - Value: resolvedUrl in 12px `--font-mono`
   - Resolved Headers section:
     - Label: "Resolved Headers:" in 12px/600
     - List: each header as `key: value` in 12px `--font-mono`
   - Resolved Body section:
     - Label: "Resolved Body:" in 12px/600
     - Monaco read-only editor with the resolved body JSON
     - Syntax highlighting for JSON
   - Warnings section (if any):
     - Yellow box listing unresolved references with strikethrough text
     - `background: rgba(251,191,36,0.1); border-left: 3px solid var(--color-warning); padding: var(--space-2)`
   - If chain hasn't run: show placeholder text "[step1.response.body.name — not yet run]" in `--color-fg-muted`
   - "Close" button at bottom, Escape key dismisses
   - Fetch resolved data: call `window.api.chains.previewResolved({ collectionId, chainId, stepIndex })` on open
   - Loading state: show "Loading…" skeleton while fetching

2. Create `src/renderer/components/Chain/ChainValidationBanner.tsx` (per UI-SPEC §7, D-22):
   - Props: `{ issues: Array<{ type: string; message: string; stepIndex?: number }>; onDismiss: () => void }`
   - Only render when issues.length > 0
   - Layout: `background: rgba(251,191,36,0.1); border-left: 3px solid var(--color-warning); padding: var(--space-2) var(--space-3); font-size: 12px; margin-bottom: var(--space-2)`
   - Header: "⚠ Chain validation failed — fix before running:" in 12px/600
   - List each issue:
     - "• Step {N}: URL is empty" for empty-url
     - "• Step {N}: {{stepM.response.body.id}} references non-existent step" for invalid-ref
     - "• Step A → Step B → Step A: circular reference" for circular-ref
   - Dismiss button `✕` at top-right corner
   - Reappears on next validation failure (controlled by parent)

3. Update `src/renderer/components/Chain/ChainEditor.tsx`:
   - Import `PreviewResolvedModal` and `ChainValidationBanner`
   - Add state: `const [previewOpen, setPreviewOpen] = useState(false)`
   - Add `ChainValidationBanner` below `ChainHeader`:
     - Pass `useChain.getState().validationIssues`
     - On dismiss: `useChain.getState().clearValidation()`
   - Add `PreviewResolvedModal`:
     - Render when previewOpen is true
     - Pass collectionId, chainId, selectedStepIndex
     - On close: setPreviewOpen(false)
   - Wire "Preview Resolved" button in ChainRequestBuilder:
     - ChainRequestBuilder already has onPreviewResolved prop from 04-02
     - ChainEditor passes `() => setPreviewOpen(true)` as onPreviewResolved
   - Before running chain: call `window.api.chains.validate({ collectionId, chainId })`
     - If validation fails: set validationIssues in useChain, show banner, don't run
     - If validation passes: proceed with run

4. Update `src/renderer/components/Chain/ChainHeader.tsx`:
   - Add keyboard shortcut: Ctrl/Cmd+Enter to run chain (when not running)
   - Add keyboard shortcut: Escape to stop chain (when running)
   - Wire shortcuts in useEffect with window.addEventListener('keydown', ...)
  </action>
  <verify>
    <automated>cd C:\Users\aleks\Desktop\postman-clone && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/renderer/components/Chain/PreviewResolvedModal.tsx` shows resolved URL, headers, body in modal
    - Preview modal fetches data via window.api.chains.previewResolved
    - Unresolved references shown in yellow with strikethrough
    - `src/renderer/components/Chain/ChainValidationBanner.tsx` shows yellow banner with issue list
    - ChainEditor validates chain before running, shows banner if issues
    - ChainEditor wires PreviewResolvedModal and ChainValidationBanner
    - Keyboard shortcuts: Ctrl+Enter to run, Escape to stop
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>
    - PreviewResolvedModal shows resolved URL, headers, body with Monaco read-only
    - Preview highlights unresolved refs in yellow with strikethrough
    - ChainValidationBanner shows validation issues before chain run
    - ChainEditor validates before running, blocks if issues found
    - Keyboard shortcuts: Ctrl+Enter run, Escape stop
    - All chain UI components integrated into ChainEditor
    - TypeScript compiles
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→main | previewResolved IPC call crosses process boundary; Zod validates |
| clipboard | Copy-path writes to system clipboard; user-initiated only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | Clipboard contents | accept | User-initiated copy only; clipboard contains user's own API data |
| T-04-08 | Tampering | Preview modal data | accept | Read-only display of resolved values; no mutation of chain state |
| T-04-SC | Tampering | npm packages | mitigate | No new npm packages in this plan |
</threat_model>

<verification>
1. Data panel shows prior step responses as expandable JSON trees
2. Copy-path button copies correct {{stepN.response.body.path}} expression
3. Preview Resolved modal shows resolved URL, headers, body
4. Unresolved references highlighted yellow in preview
5. Validation banner shows before chain run when issues exist
6. Validation banner is dismissible
7. UnresolvedRefWarning shows in step result area
8. Keyboard shortcuts work: Ctrl+Enter run, Escape stop
9. Data panel collapses/expands with chevron toggle
10. TypeScript compiles
</verification>

<success_criteria>
- MAP-01: User can pull field from earlier step response (copy-path from data panel)
- MAP-02: Mappings are explicit and editable ({{stepN.response.body.path}} syntax)
- MAP-03: Mappings resolve at chain-run time (resolver in 04-01, preview shows resolved)
- MAP-04: User can preview resolved body before running (PreviewResolvedModal)
</success_criteria>

<output>
Create `.planning/phases/04-workflow-chains-response-mapping/04-03-SUMMARY.md` when done
</output>

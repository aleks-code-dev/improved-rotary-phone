---
phase: 04-workflow-chains-response-mapping
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/renderer/styles/tokens.css
  - src/renderer/styles/theme-dark.css
  - src/renderer/components/Sidebar.tsx
  - src/renderer/components/Sidebar/CollectionsTree.tsx
  - src/renderer/components/Chain/ChainEditor.tsx
  - src/renderer/components/Chain/ChainHeader.tsx
  - src/renderer/components/Chain/StepSequence.tsx
  - src/renderer/components/Chain/StepCard.tsx
  - src/renderer/components/Chain/ChainRequestBuilder.tsx
  - src/renderer/components/Chain/ChainSidebarItem.tsx
  - src/renderer/state/useChain.ts
autonomous: true
requirements:
  - CHAIN-01
  - CHAIN-02
  - CHAIN-03
  - CHAIN-04

must_haves:
  truths:
    - "User sees chains in the collection sidebar with a chain icon and step count"
    - "User can click 'New Chain' to create a chain with one empty step"
    - "Clicking a chain in the sidebar opens the chain editor in the center pane"
    - "Chain editor shows horizontal step cards with arrows (sketch 009-A)"
    - "User can click a step card to select it and edit its request spec below"
    - "User can add steps via '+' button, remove steps via context menu"
    - "Chain header shows name, step count, Run Chain / Stop buttons, progress bar"
    - "Running a chain shows per-step progress (pulse on active, green/red on complete/fail)"
    - "Reference expressions {{stepN.response.body.path}} are highlighted purple in Monaco"
    - "Chain execution state (running step, progress, results) tracked in Zustand"
  artifacts:
    - path: "src/renderer/components/Chain/ChainEditor.tsx"
      provides: "Top-level chain editor layout — header + steps + request builder"
      exports: ["ChainEditor"]
    - path: "src/renderer/components/Chain/StepSequence.tsx"
      provides: "Horizontal step cards with arrows and add button (sketch 009-A)"
      exports: ["StepSequence"]
    - path: "src/renderer/components/Chain/StepCard.tsx"
      provides: "Individual step card with status, method, URL preview"
      exports: ["StepCard"]
    - path: "src/renderer/components/Chain/ChainHeader.tsx"
      provides: "Chain name, step count, run/stop buttons, progress bar"
      exports: ["ChainHeader"]
    - path: "src/renderer/state/useChain.ts"
      provides: "Zustand store for chain execution state"
      exports: ["useChain"]
    - path: "src/renderer/styles/tokens.css"
      provides: "New chain color tokens"
      contains: "--color-chain-badge"
  key_links:
    - from: "src/renderer/components/Sidebar/CollectionsTree.tsx"
      to: "src/renderer/components/Chain/ChainSidebarItem.tsx"
      via: "renders chain items in collection tree"
      pattern: "chain.*item"
    - from: "src/renderer/components/Chain/ChainEditor.tsx"
      to: "src/renderer/state/useChain.ts"
      via: "useChain((s) => s.activeChainId)"
      pattern: "useChain"
    - from: "src/renderer/state/useChain.ts"
      to: "src/preload/index.ts"
      via: "window.api.chains.run({ collectionId, chainId })"
      pattern: "window.api.chains"
    - from: "src/renderer/components/Chain/ChainRequestBuilder.tsx"
      to: "src/renderer/components/RequestEditor.tsx"
      via: "reuses MethodPicker, SubTabs, BodyTab, HeadersTab, AuthTab, SettingsTab"
      pattern: "import.*RequestEditor"
---

<objective>
Chain editor UI + sidebar integration (CHAIN-01, CHAIN-02, CHAIN-03, CHAIN-04)

Purpose: Build the chain editor UI — sidebar chain items with "New Chain" button, horizontal step sequence (sketch 009-A), per-step request builder (reusing existing RequestEditor components), chain header with run/stop/progress, and Zustand store for chain execution state. Add Monaco reference highlighting for {{stepN.response.body.path}} expressions.

Output: ChainEditor, ChainHeader, StepSequence, StepCard, ChainRequestBuilder, ChainSidebarItem components. useChain Zustand store. Sidebar integration with chain items. CSS tokens for chain status colors.
</objective>

<execution_context>
@C:/Users/aleks/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/aleks/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-workflow-chains-response-mapping/04-CONTEXT.md
@.planning/phases/04-workflow-chains-response-mapping/04-RESEARCH.md
@.planning/phases/04-workflow-chains-response-mapping/04-UI-SPEC.md
@.planning/phases/04-workflow-chains-response-mapping/04-01-SUMMARY.md

<interfaces>
<!-- From 04-01 (created by prior plan): -->

From src/shared/schemas/collection.ts:
```typescript
export const ChainStepSchema = z.object({
  stepIndex: z.number().int().min(1),
  name: z.string().default(''),
  request: RequestSpecSchema,
  timeoutMs: z.number().int().min(1000).max(600_000).default(30_000),
  retryCount: z.number().int().min(0).max(5).default(0),
  retryDelayMs: z.number().int().min(0).max(30_000).default(1000),
  lastResult: StepResultSchema.optional(),
});
export const ChainSchema = z.object({
  id: z.string().uuid(),
  name: z.string().min(1).max(200),
  steps: z.array(ChainStepSchema),
  createdAt: z.number(),
});
export type Chain = z.infer<typeof ChainSchema>;
export type ChainStep = z.infer<typeof ChainStepSchema>;
export type StepResult = z.infer<typeof StepResultSchema>;
```

From src/preload/index.ts (added in 04-01):
```typescript
chains: {
  create: (args: { collectionId: string; name?: string }) => Promise<any>;
  update: (args: { collectionId: string; chainId: string; chain: any }) => Promise<any>;
  delete: (args: { collectionId: string; chainId: string }) => Promise<any>;
  run: (args: { collectionId: string; chainId: string }) => Promise<any>;
  stop: (args: { chainId: string }) => Promise<any>;
  validate: (args: { collectionId: string; chainId: string }) => Promise<any>;
  onProgress: (cb: (data: any) => void) => () => void;
  onStepResult: (cb: (data: any) => void) => () => void;
  onComplete: (cb: (data: any) => void) => () => void;
}
```

From src/renderer/components/RequestEditor.tsx (existing — reuse components):
```typescript
import { MethodPicker } from './RequestEditor/MethodPicker';
import { SubTabs, type SubTab } from './RequestEditor/SubTabs';
import { ParamsTab } from './RequestEditor/ParamsTab';
import { HeadersTab } from './RequestEditor/HeadersTab';
import { BodyTab } from './RequestEditor/BodyTab';
import { AuthTab } from './RequestEditor/AuthTab';
import { SettingsTab } from './RequestEditor/SettingsTab';
```

From src/renderer/styles/tokens.css:
```css
:root {
  --color-method-get: oklch(0.7 0.15 230);
  --color-method-post: oklch(0.7 0.15 145);
  --color-accent: oklch(0.55 0.18 230);
  --space-1: 4px; --space-2: 8px; --space-3: 12px; --space-4: 16px;
  --font-mono: ui-monospace, "Cascadia Code", "JetBrains Mono", Menlo, monospace;
  --radius-1: 4px; --radius-2: 8px;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS tokens + useChain store + sidebar chain items</name>
  <read_first>
    src/renderer/styles/tokens.css,
    src/renderer/styles/theme-dark.css,
    src/renderer/components/Sidebar/CollectionsTree.tsx,
    src/renderer/store/collections.ts,
    src/renderer/hooks/useCollections.ts,
    src/shared/schemas/collection.ts,
    src/preload/index.ts
  </read_first>
  <files>
    src/renderer/styles/tokens.css,
    src/renderer/styles/theme-dark.css,
    src/renderer/state/useChain.ts,
    src/renderer/components/Chain/ChainSidebarItem.tsx,
    src/renderer/components/Sidebar/CollectionsTree.tsx
  </files>
  <action>
1. Add chain color tokens to `src/renderer/styles/tokens.css` (per UI-SPEC §Phase 4 New Color Tokens):
   - `--color-chain-badge: oklch(0.55 0.2 290);` (PATCH purple)
   - `--color-ref-highlight: oklch(0.55 0.2 290 / 0.2);` (purple at 20% opacity)
   - `--color-ref-text: oklch(0.55 0.2 290);`
   - `--color-step-active: oklch(0.7 0.15 230);` (blue)
   - `--color-step-success: oklch(0.65 0.18 145);` (green)
   - `--color-step-failed: oklch(0.65 0.18 25);` (red)
   - `--color-step-pending: oklch(0.45 0.01 250);` (gray)
   - `--color-chain-bar-bg: oklch(0.55 0.2 290 / 0.08);`
   - `--color-progress-bar: oklch(0.7 0.15 145);` (accent orange)
   - Add `.chain-ref-highlight` CSS class:
     ```css
     .chain-ref-highlight {
       background: var(--color-ref-highlight);
       color: var(--color-ref-text);
       font-weight: 600;
       padding: 1px 4px;
       border-radius: 2px;
     }
     ```

2. Create `src/renderer/state/useChain.ts` (Zustand store for chain execution state):
   - Interface `ChainState`:
     - `activeChainId: string | null` — currently open chain
     - `activeCollectionId: string | null` — collection containing the chain
     - `selectedStepIndex: number` — 1-based, default 1
     - `isRunning: boolean`
     - `isStopping: boolean`
     - `progress: { stepIndex: number; totalSteps: number; status: string } | null`
     - `stepResults: Map<number, any>` — stepIndex → StepRunResult from orchestrator
     - `validationIssues: Array<{ type: string; message: string; stepIndex?: number }> | null`
   - Actions:
     - `openChain(collectionId: string, chainId: string)` — sets active chain
     - `closeChain()` — clears active chain
     - `selectStep(stepIndex: number)` — sets selected step
     - `setRunning(running: boolean)` — sets isRunning
     - `setStopping(stopping: boolean)` — sets isStopping
     - `updateProgress(data: any)` — updates progress from chains:progress event
     - `updateStepResult(data: any)` — updates stepResults from chains:stepResult event
     - `setComplete(data: any)` — clears isRunning, updates final status
     - `setValidationFailed(data: any)` — sets validationIssues
     - `clearValidation()` — clears validationIssues
     - `reset()` — clears all state

3. Create `src/renderer/components/Chain/ChainSidebarItem.tsx`:
   - Props: `{ chain: { id: string; name: string; steps: any[] }; collectionId: string; isActive: boolean; onClick: () => void }`
   - Render: chain icon `🔗` + chain name + step count badge `(N)`
   - Badge styling: `background: var(--color-chain-badge)`, `color: #fff`, `font-size: 9px`, `padding: 1px 6px`, `border-radius: var(--radius-full)`, `font-weight: 600`
   - Item styling: same as request tree items (12px Inter, `--color-fg`)
   - Active state: `background: var(--color-bg-hover)`, `font-weight: 600`
   - Right-click context menu: Rename, Duplicate, Delete (use native contextMenu or simple div)

4. Extend `src/renderer/components/Sidebar/CollectionsTree.tsx`:
   - Import `ChainSidebarItem`
   - When a collection is expanded and fullCollection is loaded:
     - Render chains from `fullCollection.chains ?? []` after the request items
     - Each chain renders a `ChainSidebarItem`
     - Clicking a chain calls `useChain.getState().openChain(col.id, chain.id)`
   - Add "New Chain" button in the collection header (alongside existing "+" button):
     - Button: text icon `🔗` with tooltip "New chain"
     - On click: call `window.api.chains.create({ collectionId: col.id, name: 'New Chain' })`
     - Invalidate collections query on success
   - When a chain is active, highlight it in the sidebar
  </action>
  <verify>
    <automated>cd C:\Users\aleks\Desktop\postman-clone && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/renderer/styles/tokens.css` contains `--color-chain-badge`, `--color-ref-highlight`, `--color-step-active`, `--color-step-success`, `--color-step-failed`, `--color-step-pending`
    - `src/renderer/styles/tokens.css` contains `.chain-ref-highlight` class with purple background
    - `src/renderer/state/useChain.ts` exports `useChain` with activeChainId, selectedStepIndex, isRunning, progress, stepResults
    - `src/renderer/components/Chain/ChainSidebarItem.tsx` renders chain icon + name + step count badge
    - `src/renderer/components/Sidebar/CollectionsTree.tsx` renders chain items and "New Chain" button
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>
    - Chain color tokens added to tokens.css (badge, ref-highlight, step states, progress bar)
    - .chain-ref-highlight CSS class for Monaco decorations
    - useChain Zustand store with execution state (activeChainId, selectedStepIndex, isRunning, progress, stepResults)
    - ChainSidebarItem component with chain icon, name, step count badge
    - CollectionsTree renders chain items and "New Chain" button
    - TypeScript compiles
  </done>
</task>

<task type="auto">
  <name>Task 2: ChainEditor + ChainHeader + StepSequence + StepCard + ChainRequestBuilder</name>
  <read_first>
    src/renderer/components/RequestEditor.tsx,
    src/renderer/components/RequestEditor/MethodPicker.tsx,
    src/renderer/components/RequestEditor/SubTabs.tsx,
    src/renderer/components/RequestEditor/BodyTab.tsx,
    src/renderer/state/useChain.ts,
    src/shared/schemas/collection.ts,
    src/preload/index.ts,
    .planning/phases/04-workflow-chains-response-mapping/04-UI-SPEC.md
  </read_first>
  <files>
    src/renderer/components/Chain/ChainEditor.tsx,
    src/renderer/components/Chain/ChainHeader.tsx,
    src/renderer/components/Chain/StepSequence.tsx,
    src/renderer/components/Chain/StepCard.tsx,
    src/renderer/components/Chain/ChainRequestBuilder.tsx
  </files>
  <action>
1. Create `src/renderer/components/Chain/StepCard.tsx` (per UI-SPEC §2 Step Sequence):
   - Props: `{ step: ChainStep; isSelected: boolean; isActive: boolean; result?: StepRunResult; onClick: () => void; onDoubleClick: () => void }`
   - Layout: step number circle (24px) + method badge + URL preview + status line
   - Step number circle: `width: 24px; height: 24px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 11px; font-weight: 700; color: #fff`
   - Circle background by state:
     - Active/selected: `var(--color-step-active)`
     - Completed: `var(--color-step-success)`
     - Failed: `var(--color-step-failed)`
     - Pending: `var(--color-step-pending)`
   - Method badge: 10px/700, colored per method (use `var(--color-method-get)` etc.), `border-radius: var(--radius-1)`, `padding: 1px 6px`
   - URL preview: 11px `--font-mono`, `--color-fg-muted`, `max-width: 120px`, truncated with ellipsis
   - Status line: 9px, color varies by state
     - Not run: "○ Not run" in `--color-step-pending`
     - Running: "◆ Running…" with spinner in `--color-step-active`
     - Success: "✓ {status} · {time}ms" in `--color-step-success`
     - Failed: "✗ {status/error} · {time}ms" in `--color-step-failed`
     - Retrying: "↻ Retry {N}/{max}…" in `--color-step-active`
   - Card border by state:
     - Selected: `border: 2px solid var(--color-step-active)`, `background: rgba(76,154,255,0.06)`
     - Running: `border: 2px solid var(--color-step-active)`, pulse animation (opacity oscillation 1s)
     - Failed: `border: 2px solid var(--color-step-failed)`
     - Default: `border: 2px solid var(--color-border)`
   - Min-width: 150px, flex-shrink: 0
   - On click: call onClick (selects this step)
   - On double-click: call onDoubleClick (edit name inline)

2. Create `src/renderer/components/Chain/StepSequence.tsx` (per UI-SPEC §2):
   - Props: `{ steps: ChainStep[]; selectedStepIndex: number; results: Map<number, any>; runningStepIndex: number | null; onSelectStep: (index: number) => void; onAddStep: () => void; onRemoveStep: (index: number) => void }`
   - Layout: `display: flex; overflow-x: auto; gap: var(--space-1); padding: var(--space-3) var(--space-4); align-items: center; background: var(--color-bg); border-bottom: 1px solid var(--color-border)`
   - Render each step as a `StepCard` with `→` arrow between them
   - Arrow: `→` character, `color: var(--color-fg-muted)`, `font-size: 18px`, `padding: 0 var(--space-1)`
   - Last element: add step button (dashed border card)
     - `border: 2px dashed var(--color-border)`, `min-width: 150px`, same height as step cards
     - Centered `+` character, `font-size: 20px`, `color: var(--color-fg-muted)`
     - Hover: `border-color: var(--color-step-active)`, `color: var(--color-step-active)`
     - Click: calls onAddStep
   - Right-click on step card: context menu with "Rename", "Duplicate", "Remove Step"
   - Remove step confirmation: "This step and its mappings will be removed. References from later steps will break."

3. Create `src/renderer/components/Chain/ChainHeader.tsx` (per UI-SPEC §1):
   - Props: `{ chain: Chain; isRunning: boolean; isStopping: boolean; progress: any; onSave: () => void; onRun: () => void; onStop: () => void; onNameChange: (name: string) => void }`
   - Layout: `background: var(--color-bg-elevated); border-bottom: 1px solid var(--color-border); padding: var(--space-3) var(--space-4)`
   - First row:
     - Chain icon `🔗` + chain name (editable inline — click to edit, Enter/blur to save)
     - Chain badge: `background: var(--color-chain-badge)`, `color: #fff`, `font-size: 10px`, `padding: 2px 8px`, `border-radius: var(--radius-full)`, `font-weight: 600` — shows "CHAIN"
     - Step count: "{N} steps" in `--color-fg-muted`, 12px
     - Spacer
     - "Save" button: `background: var(--color-accent)`, `color: #fff`, 12px, `padding: 4px 12px`, `border-radius: var(--radius-1)`
     - "Run Chain" button: same accent styling, disabled when isRunning or no steps
       - When running: change to "Stop" button with `background: var(--color-step-failed)`
   - Second row (only when running):
     - Step indicator: "Step {N} of {M} · {status}" in 12px `--color-fg-muted`
     - Progress bar: `height: 3px; background: var(--color-border); border-radius: 2px; overflow: hidden`
     - Progress fill: `height: 100%; background: var(--color-progress-bar); transition: width 0.3s ease; width: {percent}%`

4. Create `src/renderer/components/Chain/ChainRequestBuilder.tsx` (per UI-SPEC §3):
   - Props: `{ step: ChainStep; stepIndex: number; totalSteps: number; priorStepsWithResults: Array<{ stepIndex: number; method: string; url: string; status: number }>; onStepChange: (updated: ChainStep) => void; onSendSingle: () => void }`
   - Layout: identical to main RequestEditor but scoped to a chain step
   - Chain context info bar (above URL bar):
     - "Step {N} of {M} · Available references: step1 (POST /users ✓ 201)" in 10px `--font-mono`, `--color-fg-muted`
     - If no prior data: "No previous step data available — run earlier steps first."
   - Method badge: reuse `MethodPicker` component, pass step.request.method
   - URL bar: `--font-mono` 13px, value from step.request.url, onChange updates step
   - Sub-tabs: reuse `SubTabs` component (Params, Headers, Body, Auth, Settings)
   - Body editor: reuse `BodyTab` component with tabId = `chain-${chainId}-${stepIndex}`
   - "Send" button: sends only this single step (not the whole chain) via window.api.request.send
   - "Preview Resolved" button in body toolbar: calls onPreviewResolved (opens modal in plan 04-03)
   - Monaco reference highlighting: when body editor mounts, apply deltaDecorations for `{{stepN.response.body.path}}` patterns (per RESEARCH.md §Pattern 3)
     - Regex: `/\{\{step\d+\.response\.(?:body|headers|status)[^}]*\}\}/g`
     - Decoration: `inlineClassName: 'chain-ref-highlight'`
     - Re-apply on `model.onDidChangeContent` debounced at 150ms

5. Create `src/renderer/components/Chain/ChainEditor.tsx` (per UI-SPEC §Chain Editor Layout):
   - Props: `{ collectionId: string; chainId: string }`
   - Fetch chain data via `useCollection(collectionId)` → find chain by id
   - Use `useChain` store for execution state
   - Layout (3 zones, vertical):
     1. `ChainHeader` (40px) — chain name, step count, run/stop/progress
     2. `StepSequence` (64px) — horizontal step cards
     3. `ChainRequestBuilder` (flex-grow) — request editor for selected step
   - Handle chain mutations:
     - Add step: create new empty step with stepIndex = steps.length + 1, save via window.api.chains.update
     - Remove step: remove from array, re-index remaining steps, save
     - Step change: update step in array, save (debounced 500ms)
     - Name change: update chain name, save
   - Handle chain execution:
     - Run chain: call window.api.chains.run({ collectionId, chainId }), subscribe to progress/stepResult/complete events
     - Stop chain: call window.api.chains.stop({ chainId })
     - Subscribe to events in useEffect, update useChain store
   - Handle single step send:
     - Build RequestSpec from step.request
     - Call window.api.request.send(spec)
     - Update step.lastResult with response
   - Empty state when no steps: "Add your first step — Click '+' to add a request step to this chain."
  </action>
  <verify>
    <automated>cd C:\Users\aleks\Desktop\postman-clone && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/renderer/components/Chain/StepCard.tsx` renders step number circle, method badge, URL preview, status line with correct colors per state
    - `src/renderer/components/Chain/StepSequence.tsx` renders horizontal step cards with arrows and add button
    - `src/renderer/components/Chain/ChainHeader.tsx` shows chain name, step count badge, Run/Stop buttons, progress bar
    - `src/renderer/components/Chain/ChainRequestBuilder.tsx` shows method badge, URL bar, sub-tabs, chain context info bar
    - `src/renderer/components/Chain/ChainEditor.tsx` composes Header + StepSequence + RequestBuilder
    - ChainEditor subscribes to chains:progress, chains:stepResult, chains:complete events
    - Monaco reference highlighting applies .chain-ref-highlight to {{stepN...}} patterns
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>
    - StepCard renders with correct state colors (active/running/success/failed/pending)
    - StepSequence shows horizontal cards with arrows, add button, context menu
    - ChainHeader shows name (editable), step count, Run/Stop, progress bar
    - ChainRequestBuilder reuses MethodPicker, SubTabs, BodyTab with chain context
    - ChainEditor composes all components, handles mutations and execution
    - Monaco reference highlighting with .chain-ref-highlight decoration
    - useChain store tracks execution state and subscribes to IPC events
    - TypeScript compiles
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→main | IPC payloads cross process boundary; Zod validates at handler entry |
| renderer state | Zustand store holds chain execution state (ephemeral, not persisted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | Chain step mutation via UI | mitigate | Step changes go through window.api.chains.update which validates with ChainSchema |
| T-04-06 | Information Disclosure | Chain data in renderer memory | accept | Desktop single-user tool, chain data is user's own API requests |
| T-04-SC | Tampering | npm packages | mitigate | No new npm packages in this plan — all existing deps |
</threat_model>

<verification>
1. Sidebar shows chain items with chain icon and step count badge
2. "New Chain" button creates a chain and it appears in sidebar
3. Clicking a chain opens ChainEditor in center pane
4. StepSequence shows horizontal step cards with arrows
5. Clicking a step card selects it, request builder shows that step's spec
6. Adding a step appends a new step card
7. Run Chain button triggers chain execution, progress bar advances
8. Step cards show correct status colors during execution
9. Monaco highlights {{stepN.response.body.path}} with purple background
10. TypeScript compiles
</verification>

<success_criteria>
- CHAIN-01: User can define ordered chain (StepSequence with add/remove/reorder)
- CHAIN-02: Reference expressions highlighted purple in Monaco (chain-ref-highlight)
- CHAIN-03: User can run chain and see per-step results (ChainHeader progress, StepCard status)
- CHAIN-04: User can re-run single step (orchestrator re-runs from 1 to N per 04-01)
</success_criteria>

<output>
Create `.planning/phases/04-workflow-chains-response-mapping/04-02-SUMMARY.md` when done
</output>
